---
//@ts-nocheck
import MainLayout from "../layouts/MainLayout.astro";
import Heading from '../components/Heading.astro';
import Text from '../components/Text.astro';
import Card from '../components/card.astro';

    // url and key to supabase/database
const url = "https://hrhpfuamvbbfvfhfchbv.supabase.co/rest/v1/NintendoPusheren";
const key =
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhyaHBmdWFtdmJiZnZmaGZjaGJ2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk3ODA5OTMsImV4cCI6MjA5NTM1Njk5M30.pDuU-_jF2L-ehWC0LVdEd8wuTOJprqtwgGcygfZN-zE";

const options = {
  headers: {
    apikey: key,
  },
};
// fetch data
const response = await fetch(url, options);
const data = await response.json();
const products = Array.isArray(data) ? data.filter(product => product.category === "Switch 2") : [];
console.log('Products data:', products); // DEBUG: Check what fields exist in console in browser (F12)

const {
  title,
  category,
  qty,
  price,
  desc,
  release,
  discount,
} = Astro.props;

---
<MainLayout title="Titel">


	<section id="category" class="flex flex-row flex-wrap gap-4 mx-10 mt-10">
    

    <div>

    </div>


        <div class="w-fit">
        <a href="">
            <img src="img/placeholder.png" alt="Image for category" class="w-30 h-auto">
            <div class="flex flex-row place-content-between">
            <Text>Forudbestillinger</Text>
            <Text>X</Text>
            </div>
        </a>
        </div>

        <div class="w-fit">
        <a>
            <img src="img/placeholder.png" alt="Image for category" class="w-30 h-auto">
            <div class="flex flex-row place-content-between">
            <Text>Nyt spil</Text>
            <Text>X</Text>
                
                <!-- <p>Forudbestillinger</p>
                <p>X</p> -->
            </div>
        </a>
        </div>

        <div class="w-fit">
        <a>
            <img src="img/placeholder.png" alt="Image for category" class="w-30 h-auto">
            <div class="flex flex-row place-content-between">
            <Text>På tilbud</Text>
            <Text>X</Text>
                
                <!-- <p>Forudbestillinger</p>
                <p>X</p> -->
            </div>
        </a>
        </div>

                <div class="w-fit">
        <a href="#" data-filter="console" class="cursor-pointer">
            <img src="img/placeholder.png" alt="Image for category" class="w-30 h-auto">
            <div class="flex flex-row place-content-between">
            <Text>Switch 2 konsol</Text>
            <Text>X</Text>
                
                <!-- <p>Forudbestillinger</p>
                <p>X</p> -->
            </div>
        </a>
        </div>

        <div class="w-fit">
        <a>
            <img src="img/placeholder.png" alt="Image for category" class="w-30 h-auto">
            <div class="flex flex-row place-content-between">
            <Text>Switch 2 tilbehør</Text>
            <Text>X</Text>
                
                <!-- <p>Forudbestillinger</p>
                <p>X</p> -->
            </div>
        </a>
        </div>

    </section>

    <Heading stylingclasses="mx-10 mt-10 decoration-red-50">Kategori: Switch 2-udvalg</Heading>

    <section id="products-section" class="flex flex-row flex-wrap gap-4 mx-10 mt-10 justify-between">
        {products.map((product) => (
          <div data-type={product.type}>
            <Card {product} />
          </div>
        ))}
        </section>

        <script>
          function applyFilter(filterType) {
            const products = document.querySelectorAll('[data-type]');
            
            products.forEach(product => {
              if (filterType === null || product.dataset.type === filterType) {
                product.style.display = '';
              } else {
                product.style.display = 'none';
              }
            });
          }

          // Get filter from URL on page load
          const params = new URLSearchParams(window.location.search);
          const urlFilter = params.get('type');
          if (urlFilter) {
            applyFilter(urlFilter);
          }

          // Add click handlers to filter buttons
          document.querySelectorAll('[data-filter]').forEach(button => {
            button.addEventListener('click', (e) => {
              e.preventDefault();
              const filterType = e.currentTarget.dataset.filter;
              
              // Update URL
              const newUrl = new URL(window.location);
              newUrl.searchParams.set('type', filterType);
              window.history.pushState({}, '', newUrl);
              
              // Apply filter
              applyFilter(filterType);
            });
          });
        </script>

</MainLayout>

